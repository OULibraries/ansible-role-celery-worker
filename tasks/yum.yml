---

- name: Packages for yum.
  yum:
    name: "{{ item }}"
    state: present
  with_items:
    - gcc
    - gcc-c++
    - python-devel
    - python-virtualenv
    - libffi-devel
    - openssl-devel
